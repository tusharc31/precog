hardware:
  allow_growth: True
  per_process_gpu_memory_fraction: 0.2

hydra:
  run:
    dir: /scratch/tushar.c/hardware/${now:%Y-%m}/${now:%m-%d-%H-%M-%S}_${dataset.params._name}_${bijection.class}_${main.tag}

